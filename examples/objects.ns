vehicle = Object.clone()
vehicle.speed = 10
vehicle.run = ->
  print('running!')
end

car = vehicle.clone({
  sound: 'vroom',
  price: 99
})

car.run()

print(car.speed)
print(car.sound)
print(car.price)
