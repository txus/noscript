vehicle = Object.clone()
vehicle.speed = 10
vehicle.run = ->
  Object.puts('running!')
end

car = vehicle.clone({
  sound: 'vroom',
  price: 99
})

car.run()

Object.puts(car.speed)
Object.puts(car.sound)
Object.puts(car.price)
