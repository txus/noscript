vehicle = Object.clone()
vehicle.speed = 10
vehicle.run = ->
  puts('running!')
end

car = vehicle.clone({
  sound: 'vroom',
  price: 99
})

car.run()

puts(car.speed)
puts(car.sound)
puts(car.price)
